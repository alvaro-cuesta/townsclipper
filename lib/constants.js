const EMPTY_TYPE = -1

const GROUND_TYPE = 15

const MAX_TYPE_COUNT = 15

const MAX_TYPE = 15 // Technically this should be 14 but it works in-game (with glitches)

// TODO: Is this maybe 254? Height starts at 1!
const MAX_HEIGHT = 255

module.exports = {
  EMPTY_TYPE,
  GROUND_TYPE,
  MAX_TYPE_COUNT,
  MAX_TYPE,
  MAX_HEIGHT,
}
